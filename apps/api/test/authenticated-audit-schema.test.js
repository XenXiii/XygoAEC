import fs from "node:fs"; import path from "node:path"; import test from "node:test"; import assert from "node:assert/strict";
const sql=fs.readFileSync(path.resolve(process.cwd(),"infrastructure/migrations/postgres/0004_authenticated_audit.sql"),"utf8");
test("authenticated audit migration covers persistent workspace-scoped records",()=>{for(const table of ["workspace_invitations","audit_conversations","audit_messages","audit_structured_facts","audit_fact_conflicts","audit_evidence_refs","audit_states","audit_tool_invocations","audit_canvas_snapshots"])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${table}\\b`));});
test("sensitive conversation fields are ciphertext and membership roles match product",()=>{assert.match(sql,/content_ciphertext BYTEA NOT NULL/);assert.match(sql,/value_ciphertext BYTEA NOT NULL/);assert.match(sql,/projection_ciphertext BYTEA NOT NULL/);assert.doesNotMatch(sql,/content\s+TEXT/);for(const role of ["owner","admin","staff","advisor","read_only_auditor"])assert.match(sql,new RegExp(`'${role}'`));});
