import test from "node:test";import assert from "node:assert/strict";import {createPostgresAuditRepository} from "../src/audit/postgres.js";
test("postgres audit repository requires encryption and uses workspace-scoped queries",async()=>{assert.throws(()=>createPostgresAuditRepository({pool:{query(){}}}),/encryption codec/);const calls=[];const repo=createPostgresAuditRepository({pool:{async query(sql,args){calls.push({sql,args});return {rows:[]}}},encryptJson:value=>Buffer.from(JSON.stringify(value)),decryptJson:value=>JSON.parse(value.toString())});await repo.getConversation("w-a","c-a");assert.match(calls[0].sql,/workspace_id=\$1 AND id=\$2/);assert.deepEqual(calls[0].args,["w-a","c-a"]);});
